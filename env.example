OPENAI_API_KEY=
# Upstash Redis — powers the /vs/<slug> permalink cache. Create a free DB at
# https://console.upstash.com and paste the REST URL + token below.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
